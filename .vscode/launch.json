{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "node",
            "request": "launch",
            "name": "Manual Debug",
            "program": "${workspaceFolder}/app.js",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}